// Define the interface for the user entity
interface User {
  id: number;
  name: string;
  email: string;
  password: string;
  image: string;
  createdAt: string;
  resvMade: number;
}
  
export default User;